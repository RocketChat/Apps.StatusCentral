export enum IncidenStatusEnum {
    Investigating = 'investigating',
    Identified = 'identified',
    Update = 'update',
    Monitoring = 'monitoring',
    Resolved = 'resolved',
    Default = 'default',
}
